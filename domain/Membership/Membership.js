class Membership {
	constructor(id, memberId, startDate, endDate, type) {
		this.id = id;
		this.memberId = memberId;
		this.startDate = startDate;
		this.endDate = endDate;
		this.type = type; 
	}
}

module.exports = Membership; 